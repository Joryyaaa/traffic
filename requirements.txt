# --- core ---
numpy>=1.24
pandas>=2.0,<3      # madina's internals use pandas' removed `fastpath` kwarg (gone in pandas 3.0)
pyyaml>=6.0
networkx>=3.1

# --- geospatial (install geopandas via conda first, see README) ---
geopandas>=0.14
shapely>=2.0
pyogrio>=0.7
osmnx>=1.9          # optional: pulling street networks from OpenStreetMap
rasterio>=1.3        # optional: scripts/fetch_population.py (WorldPop raster clipping)

# --- RL ---
gymnasium>=0.29
stable-baselines3>=2.3
sb3-contrib>=2.3    # MaskablePPO -- used by every train.py/evaluate.py/seed_sweep.py run
torch>=2.1
tensorboard>=2.15
rich>=13.0          # train.py progress bar dependency
tqdm>=4.66          # train.py progress bar dependency

# --- dev ---
pytest>=8.0
black>=24.0
ruff>=0.4
matplotlib>=3.8    # scripts/plot_closures.py, plot_flow.py, record_episode.py, reward_breakdown.py
imageio-ffmpeg>=0.5  # bundled ffmpeg binary used by FFMpegWriter MP4 scenario recordings

# --- flow simulation backend ---
# Runtime dependencies omitted by the safe --no-deps Madina install below.
pydeck
Rtree
psutil
ipython
ipykernel
# Madina is on TestPyPI:
#   python -m pip install --no-deps -i https://test.pypi.org/simple/ madina==0.0.15
