# --- core ---
numpy>=1.24
pandas>=2.0,<3      # madina's internals use pandas' removed `fastpath` kwarg (gone in pandas 3.0)
pyyaml>=6.0
networkx>=3.1

# --- geospatial (install geopandas via conda first, see README) ---
geopandas>=0.14
shapely>=2.0
pyogrio>=0.7
osmnx>=1.9          # optional: pulling street networks from OpenStreetMap

# --- RL ---
gymnasium>=0.29
stable-baselines3>=2.3
sb3-contrib>=2.3    # MaskablePPO -- used by every train.py/evaluate.py/seed_sweep.py run
torch>=2.1
tensorboard>=2.15

# --- dev ---
pytest>=8.0
black>=24.0
ruff>=0.4
matplotlib>=3.8    # scripts/plot_closures.py, plot_flow.py, record_episode.py, reward_breakdown.py

# --- flow simulation backend ---
# Madina is on TestPyPI:
#   pip install -i https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple madina
